import 'package:flutter/material.dart';

// TODO: Implement URLWidget
class URLWidget extends StatelessWidget {
  const URLWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
