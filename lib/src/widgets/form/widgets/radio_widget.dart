import 'package:flutter/material.dart';

// TODO: Implement RadioWidget
class RadioWidget extends StatelessWidget {
  const RadioWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
