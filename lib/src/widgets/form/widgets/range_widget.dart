import 'package:flutter/material.dart';

class RangeWidget extends StatelessWidget {
  const RangeWidget({Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
