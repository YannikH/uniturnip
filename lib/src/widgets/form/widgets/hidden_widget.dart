import 'package:flutter/material.dart';

// TODO: Implement HiddenWidget
class HiddenWidget extends StatelessWidget {
  const HiddenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
