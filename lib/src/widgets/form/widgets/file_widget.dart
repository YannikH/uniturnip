import 'package:flutter/material.dart';

// TODO: Implement FileWidget
class FileWidget extends StatelessWidget {
  const FileWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
